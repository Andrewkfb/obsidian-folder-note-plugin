{
  "name": "folder-note-plugin",
  "version": "0.8.0",
  "description": "Folder notes for Obsidian \u2014 modernised fork of xpgo/obsidian-folder-note-plugin.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit --skipLibCheck",
    "test": "npx esbuild test/spec.ts --bundle --platform=node --format=cjs --outfile=test/bundle.cjs --alias:obsidian=./test/obsidian-stub.cjs --log-level=error && node test/bundle.cjs && npx esbuild test/spec-decor.ts --bundle --platform=node --format=cjs --outfile=test/bundle-decor.cjs --alias:obsidian=./test/obsidian-stub.cjs --log-level=error && node test/bundle-decor.cjs"
  },
  "license": "MIT",
  "dependencies": {
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "latest",
    "tslib": "^2.8.1",
    "typescript": "^5.7.2"
  }
}
